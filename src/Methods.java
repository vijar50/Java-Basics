
public class Methods {
	//void returns nothing, can change to String and return a string, similar for int 
	public int validateHeader() {
		System.out.println("header validated."); // Printed in Parent because we have specified so
		return 2; //Needs to be explicitly printed in Parent to see
	}
}
